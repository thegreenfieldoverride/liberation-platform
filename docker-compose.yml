# Liberation Platform - Local Development
# 
# This is a convenience wrapper for local development.
# For full control, use the files in infrastructure/docker/compose/
#
# Usage:
#   docker compose up                    # Just platform (port 3333)
#   docker compose --profile full up     # Full stack
#   docker compose --profile mail up     # With mail service
#
# See infrastructure/README.md for full documentation.

include:
  - infrastructure/docker/compose/base.yml
  - infrastructure/docker/compose/development.yml
