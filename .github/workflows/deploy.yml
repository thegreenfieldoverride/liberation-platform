name: 🚫 Deploy (DISABLED)

# This workflow is disabled and should not run
# Using deploy-blue-green.yml instead

on:
  # Completely disabled - no triggers
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled'
        default: 'true'

jobs:
  disabled:
    name: 🚫 Workflow Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "❌ This workflow is disabled"
          echo "✅ Use deploy-blue-green.yml instead"
          exit 1